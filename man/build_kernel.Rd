% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_utils.R
\name{build_kernel}
\alias{build_kernel}
\title{Build a diffusion kernel from an igraph network}
\usage{
build_kernel(network, ...)
}
\arguments{
\item{network}{An undirected igraph object with named vertices.}

\item{...}{Additional arguments passed to
\code{\link[diffuStats]{regularisedLaplacianKernel}} (e.g. \code{sigma2},
\code{add_diag}, \code{normalized}).}
}
\value{
A square numeric kernel matrix with row and column names matching the
  vertex names of \code{network}.
}
\description{
Thin wrapper around \code{\link[diffuStats]{regularisedLaplacianKernel}}.
Validates the input and warns if the network is large.
}
\examples{
library(igraph)
g <- make_ring(5)
V(g)$name <- as.character(1:5)
K <- build_kernel(g)
dim(K)
}
